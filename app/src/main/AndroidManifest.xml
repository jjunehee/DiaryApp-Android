<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diaryapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_diary"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_diary_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiaryApp">
        <activity
            android:name=".LaunchActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>
        <activity android:name=".EditpageActivity" android:exported="true">

        </activity>

        <activity android:name=".ReadpageActivity" android:exported="true">

        </activity>
    </application>
</manifest>